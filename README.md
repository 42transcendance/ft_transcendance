# 42_pixel_fight

## Pour lancer le test

Lancer le serveur en local :
`npx serve .`

Puis lancer le compilateur :
`npx tsc --watch`

ici `--watch`
Permet de track les erreurs de compilations en temps reel.

Puis se rendre sur la page :
`localhost:3000`
