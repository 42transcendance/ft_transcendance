global:
  resolve_timeout: 5m

route:
  receiver: 'discord'
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'discord'
    webhook_configs:
      - url: 'http://discord-alertmanager:9094'
        send_resolved: true
